name: Release

on:
  push:
    branches:
    - develop

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        goos: [windows, linux, darwin]
        goarch: [amd64, arm64]
        project: [bluengo-prueba-jvs] # Lista de tus proyectos

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.23.5

      - name: Verify Go module
        run: |
          cd ${{ matrix.project }}
          go mod tidy
          go mod download

      - name: Create output directory
        run: mkdir -p bin

      - name: Build
        run: |
          cd ${{ matrix.project }}
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o ../bin/${{ matrix.project }}_${{ matrix.goos }}_${{ matrix.goarch }}/app main.go

      - name: Generate unique name for ZIP
        id: zip_name
        run: echo "ZIP_NAME=app_${{ matrix.project }}_${{ matrix.goos }}_${{ matrix.goarch }}.zip" >> $GITHUB_ENV

      - name: Zip executable
        run: |
          cd bin
          zip -j $ZIP_NAME app_${{ matrix.project }}_${{ matrix.goos }}_${{ matrix.goarch }}

      - name: Upload artifact (ZIP)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ZIP_NAME }}
          path: bin/${{ env.ZIP_NAME }}

  release:
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v3
        with:
          path: artifacts

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1.1.4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: "1.1.0"
          release_name: "Release 1.1.0"
          body: "Release de ejecutables versión 1.1.0"
          draft: false
          prerelease: false

      - name: Upload individual release assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Listando archivos ZIP a subir:"
          ls -lah artifacts
          for file in artifacts/*.zip; do
            echo "Subiendo $file..."
            gh release upload 1.1.0 "$file" --clobber
          done